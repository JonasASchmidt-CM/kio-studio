import type { IconProps } from './types'

/**
 * "Notifications" (bell) — a Material motif, hand-authored as our own SVG
 * (DECISIONS.md D5). `currentColor` lets it inherit text color and theme from
 * tokens. Decorative by default; pass an accessible label on the control.
 */
export function NotificationsIcon({ size = 24, ...props }: IconProps) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="currentColor"
      aria-hidden="true"
      focusable="false"
      {...props}
    >
      <path d="M12 22c1.1 0 2-.9 2-2h-4a2 2 0 0 0 2 2zm6-6v-5c0-3.07-1.64-5.64-4.5-6.32V4a1.5 1.5 0 0 0-3 0v.68C7.64 5.36 6 7.92 6 11v5l-2 2v1h16v-1l-2-2z" />
    </svg>
  )
}
